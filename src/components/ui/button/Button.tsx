/**
 * Base <button>. Variants: 'primary' (accent fill), 'secondary' (outline), 'ghost' (text only).
 * Sizes: 'sm' | 'md' | 'lg'. Props: variant, size, icon?, iconPosition?, loading?, ...ButtonHTMLAttributes.
 */

export {};
